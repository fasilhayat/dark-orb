# Production overlay — DB + API only, no host ports exposed.
# Start: make up ENV=prod  (or make up with no .env file)

services:
  battle-arena-api:
    environment:
      ASPNETCORE_ENVIRONMENT: Production
